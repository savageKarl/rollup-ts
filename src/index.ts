/**
 * @packageDocumentation
 *	rollup-ts is boilerplate, used to reuse code .
 */

/**
 * what do you  want
 *
 * @param x - fisrt number
 * @param y -  secode number
 * @returns
 */
export function sum(x: number, y: number) {
	return x + y
}

/**
 * hi
 * @param msg -  i don't konw
 * @returns whtat you say?
 */
export function say(msg: string) {
	return msg
}
